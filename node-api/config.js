const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "192.168.100.9",
      user: "api-user",
      password: "7fFbseJS",
      database: "RBM",
    },
    listPerPage: 10,
  };
module.exports = config;